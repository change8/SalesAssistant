name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script: |
            set -euo pipefail
            if [ ! -d /opt/sales-assistant/app ]; then
              echo "Application directory missing. Has deploy.sh been run?" >&2
              exit 1
            fi
            cd /opt/sales-assistant/app
            # Force HTTP/1.1 to avoid HTTP/2 stream errors
            sudo -u salesassistant git config --global http.version HTTP/1.1
            sudo ./update.sh
