<view class="container">
  <!-- Tabs (Moved to Top) -->
  <view class="tabs">
    <view 
      wx:for="{{tabs}}" 
      wx:key="index"
      class="tab-item {{activeTab === index ? 'active' : ''}}"
      data-index="{{index}}"
      bindtap="switchTab"
    >
      {{item}}
    </view>
  </view>

  <!-- Search Bar -->
  <view class="search-bar">
    <input 
      class="search-input" 
      placeholder="{{placeholderText}}"
      value="{{searchQuery}}"
      bindinput="onSearchInput"
      bindconfirm="performSearch"
      confirm-type="search"
    />
    <view wx:if="{{searchQuery}}" class="clear-btn" bindtap="onClearSearch">
      <icon type="clear" size="14" color="#d1d5db"/>
    </view>
    <view class="filter-btn" bindtap="openFilter">
      <image wx:if="{{hasActiveFilters}}" src="../../assets/filter_active.png" class="filter-icon-img" mode="aspectFit"></image>
      <image wx:else src="../../assets/filter_gray.png" class="filter-icon-img" mode="aspectFit"></image>
    </view>
  </view>

  <!-- Quick Tags -->
  <scroll-view scroll-x class="quick-tags" enable-flex>
    <!-- Contract Tags -->
    <block wx:if="{{activeTab === 0}}">
      <view class="chip {{quickTags.fp ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="fp">只看 FP</view>
      <view class="chip {{quickTags.time === '3' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="time" data-value="3">近 3 年</view>
      <view class="chip {{quickTags.time === '5' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="time" data-value="5">近 5 年</view>
      <view class="chip {{quickTags.amount === '300' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="amount" data-value="300">300万+</view>
      <view class="chip {{quickTags.amount === '500' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="amount" data-value="500">500万+</view>
      <view class="chip {{quickTags.amount === '800' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="amount" data-value="800">800万+</view>
      <view class="chip {{quickTags.amount === '1000' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="amount" data-value="1000">1000万+</view>
      <view class="chip {{quickTags.status === 'completed' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="status" data-value="completed">只看完结</view>
    </block>

    <!-- Qualification Tags -->
    <block wx:if="{{activeTab === 1}}">
      <view class="chip {{quickTags.group === '1100' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="group" data-value="1100">只看集团 (1100)</view>
      <view class="chip {{quickTags.notExpired ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="notExpired">未过期</view>
    </block>

    <!-- IP Tags -->
    <block wx:if="{{activeTab === 2}}">
      <view class="chip {{quickTags.group === '1100' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="group" data-value="1100">只看集团 (1100)</view>
      <view class="chip {{quickTags.notExpired ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="notExpired">未过期</view>
      <view class="chip {{quickTags.ipCategory === 'patent' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="ipCategory" data-value="patent">专利</view>
      <view class="chip {{quickTags.ipCategory === 'copyright' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="ipCategory" data-value="copyright">软著</view>
      <view class="chip {{quickTags.ipCategory === 'trademark' ? 'active' : ''}}" bindtap="toggleQuickTag" data-type="ipCategory" data-value="trademark">商标</view>
    </block>
  </scroll-view>

  <!-- Results -->
  <view class="results">
    <!-- Loading -->
    <view wx:if="{{loading && results.length === 0}}" class="loading">
      <text>加载中...</text>
    </view>

    <!-- Empty -->
    <view wx:elif="{{!loading && results.length === 0}}" class="empty">
      <text class="icon">🔍</text>
      <text class="message">{{searchQuery ? '未找到相关结果' : '输入关键词开始搜索'}}</text>
      
      <!-- Helper Text -->
      <view class="search-helper" wx:if="{{!searchQuery}}">
        <text wx:if="{{activeTab === 0}}">可筛选 2015--2025 十年内所有合同</text>
        <text wx:elif="{{activeTab === 1}}">可筛选所有资质</text>
        <text wx:elif="{{activeTab === 2}}">可筛选所有知识产权</text>
        <text wx:elif="{{activeTab === 3}}">仅有小部分人员信息，若搜索不到相关证书，请联系投标管理部“陈晨、张妍”进行查询</text>
      </view>
    </view>

    <!-- Contract Results -->
    <block wx:if="{{activeTab === 0}}">
      <view wx:for="{{results}}" wx:key="id" class="card" bindtap="showContractDetail" data-item="{{item}}">
        <view class="card-header">
          <view class="title-row">
            <text wx:if="{{item.short_type}}" class="tag-mini">{{item.short_type}}</text>
            <text class="title">{{item.contract_title || item.project_name || '未命名合同'}}</text>
          </view>
          <view class="copy-btn" catchtap="copyItem" data-item="{{item}}">复制</view>
        </view>
        <view class="card-body">
          <view class="info">
            <view class="info-row">
              <text class="info-label">🏢 客户:</text>
              <text class="info-value">{{item.display_customer}}</text>
            </view>
            <view class="info-row">
              <text class="info-label">💰 金额:</text>
              <text class="info-value amount-text">{{item.contract_amount || '-'}}</text>
            </view>
            <view class="info-row">
              <text class="info-label">📅 签订:</text>
              <text class="info-value">{{item.signing_date || '-'}}</text>
            </view>
          </view>
        </view>
      </view>
    </block>

    <!-- Contract Detail Popup -->
    <view class="detail-popup {{showDetail ? 'open' : ''}}" wx:if="{{activeTab === 0}}">
      <view class="detail-mask" bindtap="closeContractDetail"></view>
      <view class="detail-content">
        <view class="detail-header">
          <text>合同详情</text>
          <view class="close-btn" bindtap="closeContractDetail">×</view>
        </view>
        <scroll-view scroll-y class="detail-body">
          <view class="detail-title">{{selectedContract.contract_title || selectedContract.project_name}}</view>
          
          <view class="detail-tags">
            <text class="tag tag-primary">{{selectedContract.contract_number || '无编号'}}</text>
            <text wx:if="{{selectedContract.project_code}}" class="tag tag-primary">{{selectedContract.project_code}}</text>
            <text wx:if="{{selectedContract.contract_status}}" class="tag tag-success">{{selectedContract.contract_status}}</text>
            <text wx:if="{{selectedContract.contract_type}}" class="tag tag-secondary">{{selectedContract.contract_type}}</text>
          </view>

          <view class="detail-section">
            <view class="detail-row" wx:if="{{selectedContract.project_code}}">
              <text class="label">🔢 项目编号</text>
              <text class="value">{{selectedContract.project_code}}</text>
            </view>
            <view class="detail-row">
              <text class="label">🏢 客户名称</text>
              <text class="value">{{selectedContract.display_customer || '-'}}</text>
            </view>
            <view class="detail-row">
              <text class="label">🏭 所属行业</text>
              <text class="value">{{selectedContract.display_industry || '-'}}</text>
            </view>
            <view class="detail-row" wx:if="{{selectedContract.delivery_location}}">
              <text class="label">📍 交付地点</text>
              <text class="value">{{selectedContract.delivery_location}}</text>
            </view>
            <view class="detail-row" wx:if="{{selectedContract.delivery_team}}">
              <text class="label">👥 交付团队</text>
              <text class="value">{{selectedContract.delivery_team}}</text>
            </view>
            <view class="detail-row">
              <text class="label">📅 签订时间</text>
              <text class="value">{{selectedContract.signing_date || '-'}}</text>
            </view>
            <view class="detail-row">
              <text class="label">💰 合同金额</text>
              <text class="value highlight">{{selectedContract.contract_amount || '-'}}</text>
            </view>
             <view class="detail-row">
              <text class="label">📊 合同状态</text>
              <text class="value">{{selectedContract.contract_status || '-'}}</text>
            </view>
          </view>

          <view class="detail-section">
            <view class="section-title">简介</view>
            <view class="description">{{selectedContract.description || '无简介'}}</view>
          </view>
        </scroll-view>
        <view class="detail-footer">
          <button class="copy-full-btn" bindtap="copyItem" data-item="{{selectedContract}}">复制合同信息</button>
        </view>
      </view>
    </view>

    <!-- Qualification/IP Results -->
    <block wx:if="{{activeTab === 1 || activeTab === 2}}">
      <view wx:for="{{results}}" wx:key="id" class="card">
        <view class="card-header">{{item.qualification_name || '未命名'}}</view>
        <view class="card-body">
          <view class="tags">
            <text wx:if="{{item.company_name}}" class="tag tag-primary">{{item.company_name}}</text>
            <text wx:if="{{item.business_type}}" class="tag tag-secondary">{{item.business_type}}</text>
          </view>
          <view class="info">
            <text wx:if="{{item.qualification_level}}">级别: {{item.qualification_level}}</text>
            <text>到期: {{item.expire_date || '-'}}</text>
          </view>
        </view>
      </view>
    </block>

    <!-- Personnel Results -->
    <block wx:if="{{activeTab === 3}}">
      <view wx:for="{{results}}" wx:key="id" class="card">
        <view class="card-header">{{item.name || '未命名'}}</view>
        <view class="card-body">
          <view class="tags">
            <text wx:if="{{item.employee_no}}" class="tag tag-primary">{{item.employee_no}}</text>
            <text wx:if="{{item.company}}" class="tag tag-secondary">{{item.company}}</text>
          </view>
          <view class="info">
            <text wx:if="{{item.school}}">学校: {{item.school}}</text>
            <text wx:if="{{item.degree}}">学历: {{item.degree}}</text>
          </view>
          <view wx:if="{{item.certificates && item.certificates.length > 0}}" class="certificates">
            <text wx:for="{{item.certificates}}" wx:for-item="cert" wx:key="id" class="tag tag-warning">
              {{cert.certificate_name}}
            </text>
          </view>
        </view>
      </view>
    </block>

    <!-- Company Results -->
    <block wx:if="{{activeTab === 4}}">
      <view wx:for="{{results}}" wx:key="id" class="card" bindtap="showCompanyDetail" data-item="{{item}}">
        <view class="card-header">
           <view class="title-row">
            <text class="title">{{item.name || '未命名公司'}}</text>
          </view>
          <view class="copy-btn" catchtap="copyItem" data-item="{{item}}">复制</view>
        </view>
        <view class="card-body">
           <view class="tags">
            <text wx:if="{{item.code}}" class="tag tag-primary">{{item.code}}</text>
            <text wx:if="{{item.operating_state}}" class="tag tag-success">{{item.operating_state}}</text>
          </view>
          <view class="info">
            <view class="info-row" wx:if="{{item.nuccn}}">
              <text class="info-label">统一信用代码:</text>
              <text class="info-value">{{item.nuccn}}</text>
            </view>
             <view class="info-row" wx:if="{{item.legal_person}}">
              <text class="info-label">法人:</text>
              <text class="info-value">{{item.legal_person}}</text>
            </view>
          </view>
        </view>
      </view>
    </block>
    
    <!-- Company Detail Popup -->
    <view class="detail-popup {{showCompanyDetail ? 'open' : ''}}" wx:if="{{activeTab === 4}}">
      <view class="detail-mask" bindtap="closeCompanyDetail"></view>
      <view class="detail-content">
        <view class="detail-header">
          <text>公司详情</text>
          <view class="close-btn" bindtap="closeCompanyDetail">×</view>
        </view>
        <scroll-view scroll-y class="detail-body">
          <view class="detail-title">{{selectedCompany.name}}</view>
          
          <view class="detail-tags">
            <text wx:if="{{selectedCompany.code}}" class="tag tag-primary">{{selectedCompany.code}}</text>
            <text wx:if="{{selectedCompany.operating_state}}" class="tag tag-success">{{selectedCompany.operating_state}}</text>
          </view>

          <view class="detail-section">
             <view class="detail-row" wx:if="{{selectedCompany.nuccn}}">
              <text class="label">统一信用代码</text>
              <text class="value">{{selectedCompany.nuccn}}</text>
            </view>
            <view class="detail-row" wx:if="{{selectedCompany.legal_person}}">
              <text class="label">法定代表人</text>
              <text class="value">{{selectedCompany.legal_person}}</text>
            </view>
            <view class="detail-row" wx:if="{{selectedCompany.registered_capital}}">
              <text class="label">注册资本</text>
              <text class="value">{{selectedCompany.registered_capital}} {{selectedCompany.currency || ''}}</text>
            </view>
             <view class="detail-row" wx:if="{{selectedCompany.setup_date}}">
              <text class="label">成立日期</text>
              <text class="value">{{selectedCompany.setup_date}}</text>
            </view>
            <view class="detail-row" wx:if="{{selectedCompany.registered_address}}">
              <text class="label">注册地址</text>
              <text class="value">{{selectedCompany.registered_address}}</text>
            </view>
             <view class="detail-row" wx:if="{{selectedCompany.operating_period}}">
              <text class="label">经营期限</text>
              <text class="value">{{selectedCompany.operating_period}}</text>
            </view>
            <view class="detail-row" wx:if="{{selectedCompany.company_type}}">
              <text class="label">公司类型</text>
              <text class="value">{{selectedCompany.company_type}}</text>
            </view>
          </view>

          <view class="detail-section">
            <view class="section-title">经营范围</view>
            <view class="description">{{selectedCompany.business_scope || '无'}}</view>
          </view>
        </scroll-view>
        <view class="detail-footer">
          <button class="copy-full-btn" bindtap="copyCompanyFull" data-item="{{selectedCompany}}">复制全部信息</button>
        </view>
      </view>
    </view>

    <!-- Load More -->
    <view wx:if="{{hasMore}}" class="load-more">
      <button wx:if="{{!loading}}" class="load-more-btn" bindtap="loadMore">加载更多</button>
      <text wx:else>加载中...</text>
    </view>
  </view>

  <!-- Filter Drawer -->
  <view class="filter-drawer {{showFilter ? 'open' : ''}}">
    <view class="filter-mask" bindtap="closeFilter"></view>
    <view class="filter-content">
      <view class="filter-header">
        <text>筛选条件</text>
        <view class="close-btn" bindtap="closeFilter">×</view>
      </view>
      
      <scroll-view scroll-y class="filter-body">
        <!-- Contract Filters -->
        <block wx:if="{{activeTab === 0}}">
          <view class="filter-group">
            <view class="filter-label">行业</view>
            <input class="filter-input" placeholder="请输入行业" data-field="industry" bindinput="onFilterInput" value="{{filters.industry}}" />
          </view>
          <view class="filter-group">
            <view class="filter-label">合同类型</view>
            <picker bindchange="bindContractTypeChange" value="{{filters.contractTypeIndex}}" range="{{contractTypeOptions}}">
              <view class="filter-input picker-view">
                {{contractTypeOptions[filters.contractTypeIndex]}}
                <text class="picker-arrow">▼</text>
              </view>
            </picker>
          </view>
          <view class="filter-group">
            <view class="filter-label">客户名称</view>
            <input class="filter-input" placeholder="请输入客户名称" data-field="customer" bindinput="onFilterInput" value="{{filters.customer}}" />
          </view>
          <view class="filter-group">
            <view class="filter-label">合同状态</view>
            <view class="tag-group">
              <view class="filter-tag {{filters.status === '已完结' ? 'active' : ''}}" data-field="status" data-value="已完结" bindtap="onFilterTag">已完结</view>
              <view class="filter-tag {{filters.status === '进行中' ? 'active' : ''}}" data-field="status" data-value="进行中" bindtap="onFilterTag">进行中</view>
            </view>
          </view>
          <view class="filter-group">
            <view class="filter-label">金额范围 (万)</view>
            <view class="range-inputs">
              <input class="filter-input small" placeholder="最小" type="number" data-field="minAmount" bindinput="onFilterInput" value="{{filters.minAmount}}" />
              <text>-</text>
              <input class="filter-input small" placeholder="最大" type="number" data-field="maxAmount" bindinput="onFilterInput" value="{{filters.maxAmount}}" />
            </view>
          </view>
        </block>

        <!-- Personnel Filters -->
        <block wx:if="{{activeTab === 3}}">
          <!-- ... existing personnel filters ... -->
          <view class="filter-group">
            <view class="filter-label">状态</view>
            <view class="tag-group">
              <view class="filter-tag {{filters.personnelStatus === '在职' ? 'active' : ''}}" data-field="personnelStatus" data-value="在职" bindtap="onFilterTag">在职</view>
              <view class="filter-tag {{filters.personnelStatus === '离职' ? 'active' : ''}}" data-field="personnelStatus" data-value="离职" bindtap="onFilterTag">离职</view>
            </view>
          </view>
          <view class="filter-group">
            <view class="filter-label">公司</view>
            <input class="filter-input" placeholder="公司/社保公司" data-field="personnelCompany" bindinput="onFilterInput" value="{{filters.personnelCompany}}" />
          </view>
          <view class="filter-group">
            <view class="filter-label">学历</view>
            <view class="tag-group">
              <view class="filter-tag {{filters.personnelDegree === '本科' ? 'active' : ''}}" data-field="personnelDegree" data-value="本科" bindtap="onFilterTag">本科</view>
              <view class="filter-tag {{filters.personnelDegree === '硕士' ? 'active' : ''}}" data-field="personnelDegree" data-value="硕士" bindtap="onFilterTag">硕士</view>
              <view class="filter-tag {{filters.personnelDegree === '博士' ? 'active' : ''}}" data-field="personnelDegree" data-value="博士" bindtap="onFilterTag">博士</view>
            </view>
          </view>
          <view class="filter-group">
            <view class="filter-label">证书名称</view>
            <input class="filter-input" placeholder="请输入证书名称" data-field="personnelCertificate" bindinput="onFilterInput" value="{{filters.personnelCertificate}}" />
          </view>
        </block>

        <!-- Company Filters -->
        <block wx:if="{{activeTab === 4}}">
          <view class="filter-group">
             <view class="filter-label">经营状态</view>
             <view class="tag-group">
               <view class="filter-tag {{filters.status === '存续' ? 'active' : ''}}" data-field="status" data-value="存续" bindtap="onFilterTag">存续</view>
               <view class="filter-tag {{filters.status === '在业' ? 'active' : ''}}" data-field="status" data-value="在业" bindtap="onFilterTag">在业</view>
               <view class="filter-tag {{filters.status === '注销' ? 'active' : ''}}" data-field="status" data-value="注销" bindtap="onFilterTag">注销</view>
             </view>
          </view>
          <view class="filter-group">
            <view class="filter-label">注册资本 (万)</view>
            <view class="range-inputs">
              <input class="filter-input small" placeholder="最小" type="number" data-field="capital_min" bindinput="onFilterInput" value="{{filters.capital_min}}" />
              <text>-</text>
              <input class="filter-input small" placeholder="最大" type="number" data-field="capital_max" bindinput="onFilterInput" value="{{filters.capital_max}}" />
            </view>
          </view>
          <view class="filter-group">
            <view class="filter-label">成立时间</view>
            <view class="range-inputs">
               <picker mode="date" data-field="start_date" bindchange="bindDateChange">
                 <view class="filter-input small {{filters.start_date ? '' : 'placeholder'}}">
                   {{filters.start_date || '开始日期'}}
                 </view>
               </picker>
               <text>-</text>
               <picker mode="date" data-field="end_date" bindchange="bindDateChange">
                 <view class="filter-input small {{filters.end_date ? '' : 'placeholder'}}">
                   {{filters.end_date || '结束日期'}}
                 </view>
               </picker>
            </view>
          </view>
        </block>

        <!-- IP Filters -->
        <block wx:if="{{activeTab === 2}}">
          <view class="filter-group">
            <view class="filter-label">公司名称</view>
            <input class="filter-input" placeholder="请输入公司名称" data-field="companyName" bindinput="onFilterInput" value="{{filters.companyName}}" />
          </view>
          <view class="filter-group">
            <view class="filter-label">公司编号</view>
            <input class="filter-input" placeholder="请输入公司编号" data-field="companyNumber" bindinput="onFilterInput" value="{{filters.companyNumber}}" />
          </view>
          
          <view class="filter-group">
            <view class="filter-label">知识产权类别</view>
            <picker bindchange="bindIPCategoryChange" value="{{filters.ipCategoryIndex}}" range="{{ipCategoryOptions}}">
              <view class="filter-input picker-view">
                {{ipCategoryOptions[filters.ipCategoryIndex]}}
                <text class="picker-arrow">▼</text>
              </view>
            </picker>
          </view>

          <view class="filter-group">
            <view class="filter-label">公司业务类型</view>
            <picker bindchange="bindBusinessTypeChange" value="{{filters.businessTypeIndex}}" range="{{businessTypeOptions}}">
              <view class="filter-input picker-view">
                {{businessTypeOptions[filters.businessTypeIndex]}}
                <text class="picker-arrow">▼</text>
              </view>
            </picker>
          </view>
        </block>

        <!-- Empty State for other tabs (Qualifications) -->
        <block wx:if="{{activeTab === 1}}">
           <view class="filter-group">
            <view class="filter-label">公司业务类型</view>
            <picker bindchange="bindBusinessTypeChange" value="{{filters.businessTypeIndex}}" range="{{businessTypeOptions}}">
              <view class="filter-input picker-view">
                {{businessTypeOptions[filters.businessTypeIndex]}}
                <text class="picker-arrow">▼</text>
              </view>
            </picker>
          </view>
        </block>
      </scroll-view>

      <view class="filter-footer">
        <button class="reset-btn" bindtap="resetFilters">重置</button>
        <button class="export-btn" bindtap="exportData" wx:if="{{activeTab === 0}}">导出</button>
        <button class="apply-btn" bindtap="applyFilters">确认</button>
      </view>
    </view>
  </view>
</view>
